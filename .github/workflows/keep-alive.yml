name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *' # Run every 10 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Rule Engine
        run: curl -s https://rule-engine-rcg9.onrender.com/health
